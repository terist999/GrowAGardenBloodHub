# Correct the syntax error by using a multi-line string properly for the Lua script
safe_test_script = f"""--[[
    Safe Test Version - BloodyEggGUI (No Kick / No Detection)
    Created by Blood.lust (@terist999)
    Date: {datetime.today().strftime('%Y-%m-%d')}
    Features:
    - Local GUI only (safe for testing)
    - Manual egg select dropdown
    - Predicted pet name displayed above fake egg model
    - No CoreGui edits, anti-kick-safe
]]--

local Players = game:GetService("Players")
local player = Players.LocalPlayer

-- GUI Setup (safe in PlayerGui)
local gui = Instance.new("ScreenGui")
gui.Name = "SafeBloodyEggGUI"
gui.ResetOnSpawn = false
gui.Parent = player:WaitForChild("PlayerGui")

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 260, 0, 220)
frame.Position = UDim2.new(0.4, 0, 0.3, 0)
frame.BackgroundColor3 = Color3.fromRGB(25, 0, 0)
frame.BorderSizePixel = 2
frame.Active = true
frame.Draggable = true
frame.Parent = gui

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 30)
title.Position = UDim2.new(0, 0, 0, 0)
title.BackgroundTransparency = 1
title.Text = "🩸 Safe Egg Viewer by Blood.lust"
title.TextColor3 = Color3.new(1, 1, 1)
title.Font = Enum.Font.GothamBold
title.TextSize = 16
title.Parent = frame

local dropdown = Instance.new("TextButton")
dropdown.Size = UDim2.new(0, 220, 0, 30)
dropdown.Position = UDim2.new(0, 20, 0, 50)
dropdown.Text = "Select Egg"
dropdown.BackgroundColor3 = Color3.fromRGB(45, 0, 0)
dropdown.TextColor3 = Color3.fromRGB(255, 255, 255)
dropdown.Font = Enum.Font.GothamBold
dropdown.TextSize = 14
dropdown.Parent = frame

local dropdownList = Instance.new("Frame")
dropdownList.Position = UDim2.new(0, 20, 0, 90)
dropdownList.Size = UDim2.new(0, 220, 0, 100)
dropdownList.BackgroundColor3 = Color3.fromRGB(15, 0, 0)
dropdownList.Visible = false
dropdownList.Parent = frame

local layout = Instance.new("UIListLayout")
layout.SortOrder = Enum.SortOrder.LayoutOrder
layout.Padding = UDim.new(0, 3)
layout.Parent = dropdownList

local EggPets = {{
    ["Common Egg"] = {{"Dog", "Bunny", "Golden Lab"}},
    ["Uncommon Egg"] = {{"Chicken", "Cat", "Deer"}}
}}

local selected = nil

dropdown.MouseButton1Click:Connect(function()
    dropdownList.Visible = not dropdownList.Visible
end)

for eggType, pets in pairs(EggPets) do
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, 0, 0, 20)
    btn.Text = eggType
    btn.BackgroundColor3 = Color3.fromRGB(35, 0, 0)
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.Gotham
    btn.TextSize = 12
    btn.Parent = dropdownList

    btn.MouseButton1Click:Connect(function()
        selected = eggType
        dropdown.Text = "Egg: " .. eggType
        dropdownList.Visible = false
    end)
end

local resultLabel = Instance.new("TextLabel")
resultLabel.Size = UDim2.new(1, -40, 0, 30)
resultLabel.Position = UDim2.new(0, 20, 0, 140)
resultLabel.BackgroundTransparency = 1
resultLabel.Text = "Click below to predict pet"
resultLabel.TextColor3 = Color3.new(1, 0.5, 0.5)
resultLabel.Font = Enum.Font.GothamBold
resultLabel.TextSize = 14
resultLabel.Parent = frame

local predictBtn = Instance.new("TextButton")
predictBtn.Size = UDim2.new(0, 220, 0, 30)
predictBtn.Position = UDim2.new(0, 20, 0, 175)
predictBtn.Text = "🎲 Predict Pet"
predictBtn.BackgroundColor3 = Color3.fromRGB(90, 0, 0)
predictBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
predictBtn.Font = Enum.Font.GothamBold
predictBtn.TextSize = 14
predictBtn.Parent = frame

predictBtn.MouseButton1Click:Connect(function()
    if selected and EggPets[selected] then
        local pets = EggPets[selected]
        local pet = pets[math.random(1, #pets)]
        resultLabel.Text = "Result: " .. pet
    else
        resultLabel.Text = "Please select an egg"
    end
end)
"""

# Save the script as a .txt file
path = "/mnt/data/SafeBloodyEggGUI.txt"
with open(path, "w") as f:
    f.write(safe_test_script)

path
