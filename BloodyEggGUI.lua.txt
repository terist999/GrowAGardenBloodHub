--[[ 
    BloodyEggGUI by Blood.lust (@terist999)
    Version: Final July 2025 - Egg Detector Update
    Features:
    - Bloody UI with 🅱️ Toggle
    - Dropdown Egg Selector (Includes Primal Pets)
    - Displays predicted pet above egg upon click
    - Animation effect for result text
    - 10s Cooldown + Timer Feedback
    - Kill Switch + Version Check
    - Ready for GitHub Loadstring
]]--

local http = game:GetService("HttpService")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")

local player = Players.LocalPlayer

-- Kill Switch / Version Check
local versionURL = "https://raw.githubusercontent.com/terist999/GrowAGardenBloodHub/main/version.txt"
local currentVersion = "v1.12.0"

local success, latestVersion = pcall(function()
    return game:HttpGet(versionURL)
end)

if success and latestVersion and latestVersion ~= currentVersion then
    player:Kick("Your BloodyEggGUI is outdated. Please update from Blood.lust GitHub.")
    return
end

local killSwitchURL = "https://raw.githubusercontent.com/terist999/GrowAGardenBloodHub/main/kill.txt"
local kill = false
pcall(function()
    kill = game:HttpGet(killSwitchURL)
end)
if tostring(kill):lower():find("true") then
    player:Kick("Blood.lust has disabled this GUI. Goodbye.")
    return
end

-- Egg-Pet Database (Including Primal Pets)
local EggPets = {
    ["Common Egg"] = {"Dog", "Bunny", "Golden Lab"},
    ["Uncommon Egg"] = {"Chicken", "Cat"},
    ["Bug Egg"] = {"Dragonfly", "Giant Ant", "Caterpillar", "Snail"},
    ["Bee Egg"] = {"Queen Bee"},
    ["Rare Summer Egg"] = {"Toucan", "Flamingo", "Orangutan", "Sea Turtle"},
    ["Common Summer Egg"] = {"Starfish", "Crab", "Seagull"},
    ["Prehistoric Egg"] = {"Pterodactyl", "Raptor", "Triceratops", "Stegosaurus", "Brontosaurus", "T-Rex"},
    ["Paradise Egg"] = {"Ostrich", "Peacock", "Capybara", "Scarlet Macaw", "Mimic Octopus"},
    ["Oasis Egg"] = {"Meerkat", "Sand Snake", "Axolotl", "Hyacinth Macaw", "Fennec Fox"},
    ["Night Egg"] = {"Hedgehog", "Mole", "Night Owl", "Raccoon"},
    ["Primal Egg"] = {
        "Parasaurolophus", "Iguanodon", "Pachycephalosaurus",
        "Dilophosaurus", "Ankylosaurus", "Spinosaurus"
    },
    ["Divine Pets"] = {"Disco Bee", "Blood Owl", "Night Owl", "Raccoon", "Fennec Fox", "Queen Bee"}
}

-- UI Setup
local gui = Instance.new("ScreenGui", game.CoreGui)
gui.Name = "BloodyEggGUI"

local frame = Instance.new("Frame", gui)
frame.Size = UDim2.new(0, 260, 0, 220)
frame.Position = UDim2.new(0.4, 0, 0.3, 0)
frame.BackgroundColor3 = Color3.fromRGB(20, 0, 0)
frame.BorderSizePixel = 2
frame.Active = true
frame.Draggable = true

local title = Instance.new("TextLabel", frame)
title.Size = UDim2.new(1, 0, 0, 30)
title.BackgroundTransparency = 1
title.Text = "🩸 Egg Viewer by Blood.lust"
title.TextColor3 = Color3.new(1, 0.5, 0.5)
title.Font = Enum.Font.GothamBold
title.TextSize = 16

local toggleBtn = Instance.new("TextButton", frame)
toggleBtn.Size = UDim2.new(0, 30, 0, 30)
toggleBtn.Position = UDim2.new(1, -35, 0, 0)
toggleBtn.Text = "🅱️"
toggleBtn.BackgroundColor3 = Color3.fromRGB(60, 0, 0)
toggleBtn.TextColor3 = Color3.fromRGB(255, 0, 0)
toggleBtn.Font = Enum.Font.GothamBold
toggleBtn.TextSize = 16

local minimized = false
toggleBtn.MouseButton1Click:Connect(function()
    minimized = not minimized
    for _, v in pairs(frame:GetChildren()) do
        if v:IsA("GuiObject") and v ~= toggleBtn then
            v.Visible = not minimized
        end
    end
    frame.Size = minimized and UDim2.new(0, 50, 0, 50) or UDim2.new(0, 260, 0, 220)
end)

-- Dropdown Menu
local dropdown = Instance.new("TextButton", frame)
dropdown.Size = UDim2.new(0, 220, 0, 30)
dropdown.Position = UDim2.new(0, 20, 0, 50)
dropdown.Text = "Select Egg"
dropdown.BackgroundColor3 = Color3.fromRGB(40, 0, 0)
dropdown.TextColor3 = Color3.new(1, 1, 1)
dropdown.Font = Enum.Font.GothamBold
dropdown.TextSize = 14

local dropdownList = Instance.new("Frame", frame)
dropdownList.Position = UDim2.new(0, 20, 0, 90)
dropdownList.Size = UDim2.new(0, 220, 0, 100)
dropdownList.BackgroundColor3 = Color3.fromRGB(15, 0, 0)
dropdownList.Visible = false

local layout = Instance.new("UIListLayout", dropdownList)
layout.SortOrder = Enum.SortOrder.LayoutOrder
layout.Padding = UDim.new(0, 3)

local selectedEgg = nil
dropdown.MouseButton1Click:Connect(function()
    dropdownList.Visible = not dropdownList.Visible
end)

for eggType, pets in pairs(EggPets) do
    local btn = Instance.new("TextButton", dropdownList)
    btn.Size = UDim2.new(1, 0, 0, 20)
    btn.Text = eggType
    btn.BackgroundColor3 = Color3.fromRGB(35, 0, 0)
    btn.TextColor3 = Color3.new(1, 1, 1)
    btn.Font = Enum.Font.Gotham
    btn.TextSize = 12
    btn.MouseButton1Click:Connect(function()
        selectedEgg = eggType
        dropdown.Text = "Egg: " .. eggType
        dropdownList.Visible = false
    end)
end

-- Timer Display
local timerLabel = Instance.new("TextLabel", frame)
timerLabel.Size = UDim2.new(0, 220, 0, 20)
timerLabel.Position = UDim2.new(0, 20, 0, 130)
timerLabel.BackgroundTransparency = 1
timerLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
timerLabel.Font = Enum.Font.Gotham
timerLabel.TextSize = 14

-- Randomize Button
local cooldown = false
local spinBtn = Instance.new("TextButton", frame)
spinBtn.Text = "🎲 Click Randomize"
spinBtn.Size = UDim2.new(0, 220, 0, 30)
spinBtn.Position = UDim2.new(0, 20, 0, 160)
spinBtn.BackgroundColor3 = Color3.fromRGB(60, 0, 0)
spinBtn.TextColor3 = Color3.new(1, 1, 1)
spinBtn.Font = Enum.Font.GothamBold
spinBtn.TextSize = 14

local function createFloatingText(text, position)
    local billboard = Instance.new("BillboardGui", workspace)
    billboard.Adornee = position
    billboard.Size = UDim2.new(0, 200, 0, 50)
    billboard.AlwaysOnTop = true

    local label = Instance.new("TextLabel", billboard)
    label.Size = UDim2.new(1, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = Color3.fromRGB(255, 0, 0)
    label.TextStrokeTransparency = 0
    label.Font = Enum.Font.GothamBold
    label.TextScaled = true

    local tween = TweenService:Create(label, TweenInfo.new(1.2), {TextTransparency = 1, TextStrokeTransparency = 1})
    tween:Play()
    game.Debris:AddItem(billboard, 1.5)
end

local function cooldownTimer(seconds)
    cooldown = true
    for i = seconds, 1, -1 do
        timerLabel.Text = "Cooldown: " .. i .. "s"
        wait(1)
    end
    timerLabel.Text = ""
    cooldown = false
end

spinBtn.MouseButton1Click:Connect(function()
    if cooldown or not selectedEgg then return end

    local pets = EggPets[selectedEgg]
    if not pets then return end

    local chosenPet = pets[math.random(1, #pets)]
    createFloatingText(chosenPet, player.Character.Head)

    spawn(function()
        cooldownTimer(10)
    end)
end)

-- Final Assembly
dropdown.Parent = frame
dropdownList.Parent = frame
spinBtn.Parent = frame
timerLabel.Parent = frame

-- Done
